[project]
name = "loop-monitor"
requires-python = ">=3.13"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pyright]
include = ["server"]
pythonVersion = "3.13"
typeCheckingMode = "basic"
